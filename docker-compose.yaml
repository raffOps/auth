volumes:
  redis-data:

services:
  redis:
    build:
      context: redis
      dockerfile: Dockerfile
    command: ["/app/docker-redis-entrypoint.sh"]
    container_name: redis
    ports:
      - 6379:6379
    volumes:
      - redis-data:/data
      - ./redis:/app
